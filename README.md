# frontend_job_finder_admin
Admin Panel

## Code map (graphify)

Knowledge graph of this repo, generated locally (not committed):

```sh
uv tool install graphifyy
graphify extract --code-only .
```

Output goes to `graphify-out/`.
